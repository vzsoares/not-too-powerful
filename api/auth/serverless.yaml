service: i-say-hello
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x

functions:
  handler:
    handler: handler.handler
    events:
      - httpApi:
          path: /{proxy+}
          method: ANY
    environment:
      CLIENT_ID: TODO
      CLIENT_SECRET: TODO
      REDIRECT_URI: TODO

custom:
  esbuild:
    bundle: true
    minify: true

plugins:
  - serverless-esbuild
  - serverless-offline
