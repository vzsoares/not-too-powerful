
# RUN PLAYBOOK
ansible-playbook -i inventory.yml playbook_pull_and_compose.yml -vvv --extra-vars "@.env.playbook.json"

# bash inside image 
docker exec -it ****** bash
docker run --rm -it --entrypoint bash *****
docker run -it --entrypoint sh ****

# inside vm
sudo docker logs *****

# delete dangling images
docker rmi --force  $(docker images -f dangling=true -q)

# run app
docker run -p 4000:4000 --env-file ./server/.env notnot